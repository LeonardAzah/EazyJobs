export function Dashboard({
  activeRecipient,
  sortedDings,
  myDid,
  handleSubmit,
  noteValue,
  setNoteValue,
  setErrorMessage,
  errorMessage,
}) {
  return (
    <div className="conversation">
      <div>This is Dashboard</div>
    </div>
  );
}
